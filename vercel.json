{
    "version": 2,
    "rewrites": [
        {
            "source": "/api/socket",
            "destination": "/api/socket"
        }
    ],

    "builds": [{ "src": "next.config.js", "use": "@vercel/next" }],
    "routes": [
      {
        "src": "/api/socket",
        "dest": "/api/socket"
      }
    ]
}